---
globs: "**/i18n/locales/*.json"
---
Single source of truth: packages/core/src/i18n/locales/{en,zh,ja}.json.
Keep en/zh/ja in strict key parity — every key present in one locale must
exist in all three. Japanese uses `<wbr />` after `、` for manual line-break
hints; check apps/web/CLAUDE.md for the rendering side before changing
wrapping behavior.
